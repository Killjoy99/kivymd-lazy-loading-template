<GraphScreen>
    md_bg_color: root.theme_cls.backgroundColor

    BoxLayout:
        orientation: 'vertical'

        BoxLayout:
            id: graph_container
            orientation: 'vertical'

        MDButton:
            on_release: root.manager.back()
            MDButtonText:
                text: 'Home'
